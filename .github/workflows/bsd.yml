name: ZeusiRCd Build test

on: [push]

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: deps
      run: sudo pkg install gcc9 sqlite3 gmake gettext git bash nano libmaxminddb mariadb103-client cmake yaml-cpp qpid-cpp
    - name: configure
      run: ./configure
    - name: make
      run: make
