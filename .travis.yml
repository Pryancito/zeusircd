language: generic
matrix:
  include:
    - os: linux
      env: COMPILER_NAME=g++ CC=gcc-7 && CXX=g++-7
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
            - libssl-dev
            - libsqlite3-dev
            - libmicrohttpd-dev
            - gettext
            - libicu-dev
            - autoconf
            - automake
            - openssl
            - libtool
